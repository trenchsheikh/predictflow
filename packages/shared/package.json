{
  "name": "@predictflow/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared types and utilities for PredictFlow",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "zod": "^3.22.0",
    "viem": "^2.0.0"
  }
}

