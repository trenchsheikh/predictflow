{
  "name": "predictflow",
  "version": "0.1.0",
  "private": true,
  "description": "Institutional prediction market platform with AI agent market making",
  "author": "PredictFlow Team",
  "license": "MIT",
  "scripts": {
    "dev": "npm run dev --workspaces",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "format": "prettier --write \"**/*.{ts,tsx,js,jsx,json,md}\"",
    "install:all": "npm install && npm install --workspaces"
  },
  "devDependencies": {
    "prettier": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

